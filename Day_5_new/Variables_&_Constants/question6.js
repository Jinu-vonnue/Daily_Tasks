//qn6
//Write a function  createCounter()  that increments and prints a counter each time it’s called

let count = 0;

function createCounter() {
  count++;
  console.log(count);
}

createCounter();
createCounter();
createCounter();
